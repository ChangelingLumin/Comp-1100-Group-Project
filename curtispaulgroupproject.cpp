#include <iostream>
using namespace std; // just so we dont have to use std:: all the time
                     // it makes it look nicer

int main() // just to start us off
{
    //this is the first commit with the code
    //lets wait until we learn about switches in class
    //before we add anything new
    return 0;
}